#include "IntegerArrayQueue.h"

int main()
{
  IntegerArrayQueue queue;

  queue.enqueue(1);
  queue.printArrayQueue();

  //cout << (front - back) % size << endl;
  //cout << (back + 1) % size << endl;
}